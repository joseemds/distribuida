plugins {
	id 'java-library'
	id 'application'
}

application {
	mainClass = "br.imd.ufrn.log.RunLog"
}

run {
	args = [project.properties.getOrDefault("protocol", "tcp"), project.properties.getOrDefault("port", 9999)]
}

dependencies {
}
