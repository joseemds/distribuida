plugins {
    id 'java-library'
	id 'application'
}

application {
	mainClass = "br.imd.ufrn.server.RunServer"
}

run {
	args = [project.properties.getOrDefault("protocol", "tcp"), project.properties.getOrDefault("port", 9000)]
}

dependencies {
}
